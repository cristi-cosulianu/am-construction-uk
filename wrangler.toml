# Remove the [env.production] wrapper - use top-level configuration
name = "am-construction-uk"
compatibility_date = "2024-10-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".output"

# If you need email sending, uncomment this later
# send_email = [{ binding = "SEND_EMAIL", type = "email_service" }]

# Your existing configuration
[build]
command = "npm run build"

[build.environment]
NODE_VERSION = "18"